const fs = require('fs');
const path = require('path');
const { google } = require('googleapis');

// Note: You will need a service account JSON key file for this to work locally.
// Let's create a GAS script instead to upload these since we already have GAS access.
