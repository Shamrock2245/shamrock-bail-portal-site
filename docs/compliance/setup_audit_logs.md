# Audit Log Setup

The Audit Log system is now part of the `backend-gas` module.

## Setup
1. Open `backend-gas/SetupUtilities.js`.
2. Run `setupAuditLogSheet()`.
3. It will configure the "Shamrock Security Audit Logs" sheet automatically.
